[phases.setup]
nixPkgs = ["python312", "nodejs_20"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "npm run build",
  "python manage.py collectstatic --noinput"
]

[start]
cmd = "gunicorn turnpiece.wsgi:application"
